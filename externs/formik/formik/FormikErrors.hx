package formik;
/**
	An object containing error messages whose keys correspond to FormikValues.
	Should always be an object of strings, but any is allowed to support i18n libraries.
**/
typedef FormikErrors<Values> = Dynamic;